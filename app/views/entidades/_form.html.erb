<%= form_for(entidade) do |f| %>
  <% if entidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entidade.errors.count, "error") %> prohibited this entidade from being saved:</h2>

      <ul>
      <% entidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= f.label 'Nome', class: "col-sm-2 col-form-label col-form-label-lg" %>
    <div class="col-sm-10">
      <%= f.text_field :nome, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= f.label 'CNPJ', class: "col-sm-2 col-form-label col-form-label-lg" %>
    <div class="col-sm-10">
      <%= f.text_field :cnpj, class: "form-control" %>
    </div>
  </div>

  <div class="form-group row">
    <%= f.label 'Descrição', class: "col-sm-2 col-form-label col-form-label-lg" %>
    <div class="col-sm-10">
      <%= f.text_area :descricao, class: "form-control" %>
    </div>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit "Cadastrar", class: "btn btn-primary" %>
  </div>
<% end %>
