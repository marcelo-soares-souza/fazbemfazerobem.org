<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to "Todos", users_path, :class => "nav-link active" %>
  </li>
</ul>

<div class="tab-content">
  <div class="tab-pane active"><br/>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Nome</th>
          <th>E-Mail</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.nome, user_path(user) %></td>
          <td><%= user.email %></td>
          <td>
            <!-- <%= link_to 'Editar', edit_user_path(user), :class => 'btn btn-mini btn-success' %> -->
            <%= link_to 'Apagar', user_path(user), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-mini btn-danger' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
